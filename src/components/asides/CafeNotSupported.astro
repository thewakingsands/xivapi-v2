---
import { Aside } from '@astrojs/starlight/components'
import { getLangFromUrl, useTranslatedPath } from 'i18n/utils'

const translatedPath = useTranslatedPath(getLangFromUrl(Astro.url))

const { tag } = Astro.props
const link = translatedPath(`/docs/guides/difference${tag ? `#${tag}` : ''}`)
---

<Aside type="caution"
  >此处描述的是原版 XIVAPI 的功能，由 CAFE 提供的 XIVAPI
  服务不支持此功能。详情请参考<a href={link} target="_blank">此页面</a
  >。如果您需要此功能，请使用国际服版本。</Aside
>
